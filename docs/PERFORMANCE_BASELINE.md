# Performance baseline

For repeatable checks, record browser, device class, viewport size, pixel ratio, and whether bloom or reduced-motion settings are enabled.

Compare frame-rate behavior under the same conditions when evaluating rendering changes.
