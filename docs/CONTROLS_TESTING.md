# Controls testing

Before releasing control changes, verify pointer interaction, keyboard navigation, focus visibility, and behavior when reduced motion is enabled.

Test both desktop and touch input where supported by the browser.
